/**
 * Shows a simple alert to inform users to disable the Beyond20 browser extension.
 */
 export default function showBeyond20Warning() {
	alert(`Beyond20 detected!\n\nTo avoid errors, please disable the Beyond20 browser extension while playing with AboveVTT.`);
}